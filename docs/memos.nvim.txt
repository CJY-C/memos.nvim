*memos.nvim.txt*        Fast Memos client for Neovim

==============================================================================
CONTENTS                                                   *memos.nvim-contents*

1. Introduction                                      |memos.nvim-introduction|
2. Commands                                          |memos.nvim-commands|
3. Keymaps                                           |memos.nvim-keymaps|
4. Configuration                                     |memos.nvim-configuration|
5. Latency Test                                      |memos.nvim-latency|

==============================================================================
INTRODUCTION                                           *memos.nvim-introduction*

memos.nvim is a small Neovim client for Memos. It keeps the core path focused
on listing memos, creating memos, editing memos, and saving memo content.

Only the latest Memos /api/v1 API shape is supported. Older API compatibility,
templates, metadata editing, relations, attachments, delete flows, and fuzzy
search are intentionally outside the core implementation.

==============================================================================
COMMANDS                                                   *memos.nvim-commands*

*:Memos*
  Toggle the memo list.

*:MemosCreate*
  Open a new memo buffer.

*:MemosTemplate*
  Open the template list view.

*:MemosSave*
  Save the current memo buffer / template. This command is available in memo and
  template buffers.

*:MemosTimeUpdate2Create*
  Reset the selected list memo's update_time to its create_time.

==============================================================================
KEYMAPS                                                     *memos.nvim-keymaps*

Global keymaps are intentionally not created by the plugin. Configure them in
your plugin manager or nixvim, for example `<leader>mm` -> `:Memos`.

List buffer:
>
  <CR>           Edit selected memo / template
  o              Edit selected memo in a horizontal split
  v              Edit selected memo in a vertical split
  a              Create memo (in Normal/Archive) or create from template (in Template view)
  i              Create from template (in Template view)
  e              Edit selected template (in Template view)
  n              Create memo (in Normal/Archive) or create a new template (in Template view)
  s              Search/filter memos or templates on the server
  y              Copy selected memo ID
  c              Add relation to target memo ID
  p              Toggle selected memo pin
  D              Delete selected memo / template, or unlink relation on relation line (after confirmation)
  x              Archive or restore selected memo
  A              Toggle Normal/Archive view
  T              Toggle Normal/Template view
  V              Edit selected memo visibility
  t              Edit selected memo create time
  r              Refresh list
  .              Load next page
  ,              Load previous page
  <Tab> or zo    Toggle outgoing relations expansion (expand down)
  <S-Tab> or zi  Toggle incoming relations expansion (expand up)
  zM             Collapse all active expansions
  q              Quit list
<

When viewing templates (activated by pressing `T` or running `:MemosTemplate`),
keymaps are overridden to support template operations:
- `a` or `i` instantiates a new normal memo using the selected template (with the
  `#type/template` tag stripped).
- `e` or `<CR>` edits the selected template.
- `o` or `v` edits the selected template in a horizontal or vertical split.
- `D` deletes the template from the remote server.
- `r` refreshes the template list from the remote server.
- `s` searches templates on the remote server.
- `n` opens a new template buffer. When saved, it is created archived on the server.

The `c` relation picker keeps pinned memos selectable, but it ignores pin
priority when ordering cached memo candidates. Clipboard choices stay first,
manual ID input stays last, and memo candidates are ordered by normal recency.

By default, <CR>, :MemosCreate, and new template creation open a vertical
list+edit split in the Memos workspace. o and v explicitly choose horizontal
or vertical splits. Toggling :Memos closes or restores the whole workspace,
including an active edit pane. Set window.default_open = "enew" to restore a
single-pane default.

Inside a floating split workspace, <C-w>h/<C-w>l move between vertical panes
and <C-w>k/<C-w>j move between horizontal panes; <C-w>w always toggles the
Memos pane. Directions that do not match the layout stay inside the workspace.
<C-o> and <C-i> navigate only opened memo/template buffers, not Neovim's
global jump list. Float titles and the list header display open and unsaved
buffer counts; the header also lists short titles for dirty buffers.

Memo buffer:
>
  <leader>ms     Save memo / template
  <Esc>          Return to list
<

==============================================================================
CONFIGURATION                                       *memos.nvim-configuration*

Example:
>
  require("memos").setup({
    host = "http://127.0.0.1:5230",
    token = "your-token",
    env_file = nil,
    page_size = 50,
    timeout = 10, -- Timeout for cURL requests in seconds
    list_state = "NORMAL",
    list_order_by = "pinned desc, update_time desc",
    list_style = "default",
    auto_save = false,
    window = {
      enable_float = true,
      width = 0.85,
      height = 0.85,
      border = "rounded",
      default_open = "vsplit", -- "vsplit", "split", or "enew"
    },
    keymaps = {
      list = {
        add_memo = "a",
        new_memo = "n",
        edit_memo = "<CR>",
        edit_memo_split = "o",
        edit_memo_vsplit = "v",
        search_memos = "s",
        copy_memo_id = "y",
        add_relation = "c",
        toggle_pin = "p",
        delete_memo = "D",
        archive_memo = "x",
        toggle_archive_view = "A",
        toggle_template_view = "T",
        edit_visibility = "V",
        edit_create_time = "t",
        refresh_list = "r",
        next_page = ".",
        prev_page = ",",
        quit = "q",
        toggle_expand = "<Tab>",
        toggle_expand_incoming = "<S-Tab>",
        fold_outgoing = "zo",
        fold_incoming = "zi",
        fold_all = "zM",
      },
      buffer = {
        save = "<leader>ms",
        back_to_list = "<Esc>",
      },
    },
  })

  Note: You can map any keymap to false in setup() to disable it entirely (e.g. quit = false).
<

List search uses the Memos server-side filter parameter and issues one list
request per search. Empty search input clears the filter. It does not fetch
extra pages for local fuzzy search.

Search input supports two modes:
>
  weekly note
      -> content.contains("weekly note")

  #work
      -> "work" in tags

  weekly #work #review
      -> content.contains("weekly") && "work" in tags && "review" in tags
<
Advanced search is raw Memos CEL filter text. The plugin passes expressions
that look like CEL through unchanged, and Memos evaluates them on the server:
>
  content.contains("梦") && size(tags) == 0
  content.contains("diary") && !("work" in tags)
  content.contains("journal") && ("daily" in tags || "private" in tags)
<
In Template view, the plugin adds the template constraint around your filter,
so searching work becomes:
>
  content.contains('#type/template') && (content.contains("work"))
<

Copying a memo ID uses the memo resource name already present in the list
response, such as memos/abc123. It does not issue any API request.

Toggling pin sends one PATCH request and then refreshes the list once in the
background so server-side ordering is restored.

Saving an existing memo sends one PATCH request that updates both content and
update_time, so browser views and update_time ordering reflect Neovim edits.

Saving a new template sends one POST request with the #type/template tag in the
content, then one PATCH request to archive it because the Memos create API does
not accept archive state in the create payload.

A toggles between Normal and Archive views and sends one list request. x
archives in the Normal view or restores in the Archive view, removes the memo
from the current list immediately, and then refreshes the list once in the
background.

Editing visibility opens a vim.ui.select picker and sends one PATCH request
after choosing PRIVATE, PROTECTED, or PUBLIC.

Editing create time opens a vim.ui.input prompt with the current create_time
and sends one PATCH request after confirmation.

:MemosTimeUpdate2Create acts on the selected memo in the list, sends one PATCH
request that sets update_time to create_time, updates local cache on success,
and refreshes the list once in the background. It is useful after metadata-only
edits when update-time ordering should return to the original diary date.

Split editing also uses the memo content already present in the list response.
It does not issue any extra API request.

list_style only changes local rendering and does not issue extra API requests.
Use "default" for dated rows or "compact" for shorter rows.

The list uses an in-memory stale cache for the current Neovim session. When
cached rows exist, opening or refreshing the list shows them immediately, marks
the list as refreshing, and then replaces them after one background list
request. The cache is not written to disk.

The refresh state can be exposed in lualine without making lualine a plugin
dependency:
>
  require("lualine").setup({
    sections = {
      lualine_x = {
        require("memos").statusline,
      },
    },
  })
<

For nixvim, keep it in your lualine configuration:
>
  plugins.lualine.settings.sections.lualine_x = [
    ''
      function()
        return require("memos").statusline()
      end
    ''
  ];
<

For custom statuslines, require("memos").status() returns a table with state,
text, last_refresh_at, and last_error.

Credentials may also come from a systemd-style env file:
>
  require("memos").setup({
    env_file = "/run/secrets-rendered/memos.env",
  })
<

The file should contain:
>
  MEMOS_HOST=http://127.0.0.1:5230
  MEMOS_TOKEN=your-token
<

MEMOS_HOST must include http:// or https://.

Credential priority is explicit host/token, then env_file, then MEMOS_HOST and
MEMOS_TOKEN. The plugin does not persist accounts. Global shell environment
variables work for temporary debugging, but are not recommended for long-lived
use.

For nixvim, keep secrets out of the Nix store and point the plugin at a runtime
env file:
>
  {
    extraPlugins = [
      pkgs.vimPlugins.plenary-nvim
      # Add memos.nvim through your local plugin package or overlay.
    ];

    extraConfigLua = ''
      require("memos").setup({
        env_file = "/run/secrets-rendered/memos.env",
      })
    '';

    keymaps = [
      {
        mode = "n";
        key = "<leader>mm";
        action = "<cmd>Memos<CR>";
        options.desc = "Open Memos list";
      }
    ];
  }
<

Do not put MEMOS_TOKEN directly in Nix/nixvim config; it may enter the Nix
store. The env file must be readable by the user running Neovim.

With Nix/sops, a template such as `sops.templates."memos.env"` only generates a
secret env file. It does not enter your interactive shell automatically. Use it
from a service with:
>
  systemd.services.<service>.serviceConfig.EnvironmentFile =
    config.sops.templates."memos.env".path;
<

==============================================================================
LATENCY TEST                                             *memos.nvim-latency*

Run:
>
  MEMOS_ENV_FILE=/run/secrets-rendered/memos.env ./scripts/latency-test.sh
  ./scripts/latency-test.sh --env-file /run/secrets-rendered/memos.env
  ./scripts/latency-test.sh --env-file /run/secrets-rendered/memos.env --skip-write --runs 5
  ./scripts/latency-test.sh --env-file /run/secrets-rendered/memos.env --page-size 1 --warmup
  ./scripts/latency-test.sh --env-file /run/secrets-rendered/memos.env --skip-write --order-by "create_time desc"
  ./scripts/latency-test.sh --env-file /run/secrets-rendered/memos.env --skip-write --filter 'content.contains("todo")'
  ./scripts/cold-order-by-test.sh --env-file /run/secrets-rendered/memos.env
<

The latency script uses a real Memos API and records list/create/update timing
plus request count and curl timing details. The update step patches content
and update_time in one request. It can also use credentials
injected by a systemd service. It creates and updates a test memo unless
--skip-write is used.

Useful flags:
>
  --runs N        Run multiple rounds
  --page-size N   Compare list latency for different page sizes
  --order-by V    Compare list latency for different server-side sorting
  --filter V      Compare list latency with a Memos CEL server-side filter
  --skip-write    Only measure list requests
  --warmup        Run a lightweight auth request before measured requests
<

If connect time is high, inspect network/proxy path. If time to first byte is
high while connect is low, inspect Memos server or database work.

`scripts/cold-order-by-test.sh` runs list-only tests for several orderBy values
and sleeps 30 minutes between them by default. Use `--sleep-seconds N` for a
shorter local check.

Server-side list keepalive~

If connect time is low but time to first byte is high, the slow part is likely
the Memos server or database handling the list request. In that case, keep the
real list path warm from systemd or the server, not from the plugin.
>
  curl -fsS \
    --max-time 300 \
    --write-out "warmup http=%{http_code} total=%{time_total} connect=%{time_connect} ttfb=%{time_starttransfer}\n" \
    -H "Authorization: Bearer $MEMOS_TOKEN" \
    "$MEMOS_HOST/api/v1/memos?pageSize=1&state=NORMAL&orderBy=update_time%20desc" \
    >/dev/null
<

For NixOS/systemd, run the keepalive from a service using the same secret env
file:
>
  systemd.services.memos-list-keepalive = {
    serviceConfig = {
      Type = "oneshot";
      EnvironmentFile = config.sops.templates."memos.env".path;
      ExecStart = "${pkgs.curl}/bin/curl -fsS --max-time 300 --output /dev/null --write-out \"warmup http=%%{http_code} total=%%{time_total} connect=%%{time_connect} ttfb=%%{time_starttransfer}\\n\" -H \"Authorization: Bearer $MEMOS_TOKEN\" \"$MEMOS_HOST/api/v1/memos?pageSize=1&state=NORMAL&orderBy=update_time%20desc\"";
    };
  };

  systemd.timers.memos-list-keepalive = {
    wantedBy = [ "timers.target" ];
    timerConfig = {
      OnBootSec = "2min";
      OnUnitActiveSec = "1min";
      AccuracySec = "10s";
    };
  };
<

`pageSize=1` is enough to exercise the list path. Start with a short interval
such as 1 minute and inspect the ttfb values in journalctl; if the list path
stays hot, gradually relax the timer to 2 minutes, 5 minutes, or longer. The
plugin does not run this keepalive itself, so the default request model stays
explicit and minimal.

==============================================================================
 vim:tw=78:ts=8:ft=help:norl:
